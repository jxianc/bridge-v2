query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      id
      title
      body
      points
      user {
        id
        username
        email
        createdAt
        updatedAt
      }
      postCategory {
        id
        name
      }
      comments {
        id
        body
        points
        createdAt
        updatedAt
      }
      createdAt
      updatedAt
    }
  }
}
